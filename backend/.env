# Example backend environment variables
# For MongoDB Atlas (SRV):
# MONGO_URI=mongodb+srv://<username>:<password>@cluster0.abcd.mongodb.net/garbage_db?retryWrites=true&w=majority
# Or a standard connection string:
MONGO_URI=mongodb+srv://garbage_user:garbage123@cluster0.u0riyef.mongodb.net/?appName=Cluster0

# Optional: specify the database name separately (mongoose will use this)
DB_NAME=garbage_db

# Optional: change server port
PORT=5000
